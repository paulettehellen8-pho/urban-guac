1. Create a project directory
2. Create a virtual environment <pipenv>: cmd pipenv shell
3. Install fastapi[standard]: cmd pipenv install "fastapi[standard]"
4. Create application code
5. Run the server: cmd pipenv run fastapi dev <file name>