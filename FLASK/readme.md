Intro to flask

Flask application requirements
1. pipenv
2. python version 3.8.3 Ensure in bashrc script you have the line.()
   export PIPENV_VENV_IN_PROJECT=1
   the created virtual environment will be in the project root.

Starting point
1. Create your project folder
2. Create your virtual environment


Flask for a server side rendered application <Jinja>
   1. Create your project folder
         Flask server side

   2. Set up your flask application
   3. Create a template folder <html templates>


Client side rendering
   sending <js> to the client- (It is heavy=>slow): <catching> data is stored on the front end and there's no need to make another request

   PWA<mobile, desktop and app>

Server side rendering
  Advantage -> <Initial> Fast load times
  Disadvantage:<home to about <fetch again>>
  Scalling is limited
  SQL Injection
  Cross origin

Next JS <client and server side rendering>